fastapi==0.115.0
uvicorn[standard]==0.32.1
youtube-transcript-api==0.6.2
deep-translator==1.11.4
pydantic==2.10.3
python-dotenv==1.0.1
requests==2.32.3
```

### **Why This Works:**
- ✅ `pydantic==2.10.3` will automatically install the correct pydantic-core version
- ✅ It will choose a version WITH prebuilt wheels for Python 3.14
- ✅ No compilation needed = No Rust errors
- ✅ Deploy succeeds

### **Also Check:**
Make sure your `runtime.txt` is in the **root directory** (same level as requirements.txt):
```
python-3.11.9